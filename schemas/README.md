# Vendored Contracts

These JSON Schemas are the exact contract snapshots consumed by the runner.
The public source and release history for shared contracts lives in
[BaudBound/contracts](https://github.com/BaudBound/contracts).

Update these files only as part of a reviewed compatibility change. Runner
builds must remain reproducible and must not download moving contract files.
