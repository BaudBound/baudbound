{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-dialog",
  "description": "Minimal IPC capability for authenticated BaudBound desktop dialog windows.",
  "windows": ["desktop-dialog-*"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
