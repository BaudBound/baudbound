# BaudBound Runner Desktop

Future desktop user-session app for tray controls, imports, logs, approvals, and desktop automation support.

This app should sit on top of the runner core rather than containing script editor logic.

Current state: compiling Rust app shell that depends on `baudbound-core`.

